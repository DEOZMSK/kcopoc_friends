export default function Header() {
  return (
    <header className="w-full border-b border-white/10 bg-graphite/80 px-6 py-4 text-sand">
      <div className="mx-auto flex max-w-5xl flex-col gap-1 text-center md:flex-row md:items-center md:justify-between md:text-left">
        <span className="text-sm font-semibold uppercase tracking-[0.3em]">KCOPOC Friends</span>
        <p className="text-xs text-sand/70">
          Header placeholder — основной блок будет добавлен после подготовки дизайна.
        </p>
      </div>
    </header>
  );
}
