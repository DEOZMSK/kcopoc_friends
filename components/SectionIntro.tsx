export default function SectionIntro() {
  return (
    <section
      style={{
        padding: "2rem",
        textAlign: "center",
        margin: "0 auto",
        maxWidth: "720px",
      }}
    >
      <h2 style={{ fontSize: "1.5rem", marginBottom: "1rem" }}>SectionIntro placeholder</h2>
      <p>
        This introduction section is intentionally simple so the project can ship without design blockers.
      </p>
    </section>
  );
}
