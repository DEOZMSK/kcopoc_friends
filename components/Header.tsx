export default function Header() {
  return (
    <header
      style={{
        padding: "1.5rem",
        textAlign: "center",
        borderBottom: "1px solid #ccc",
        fontWeight: 600,
      }}
    >
      Header placeholder
    </header>
  );
}
